; Loaded only when the optional huddle Compose override mounts this file.
[huddle-phone]
exten => s,1,Answer()
 same => n,Set(TIMEOUT(absolute)=${HUDDLE_MAX_SECONDS})
 same => n,AudioSocket(${HUDDLE_CALL_ID},127.0.0.1:${HUDDLE_AUDIO_PORT})
 same => n,Hangup()
